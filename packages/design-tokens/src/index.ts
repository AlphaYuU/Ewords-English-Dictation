import tokens from "../tokens.json";

export const designTokens = tokens;
export const colors = tokens.colors;
export const typography = tokens.typography;
export const spacing = tokens.spacing;
export const radius = tokens.radius;
export const shadows = tokens.shadows;
export const buttonSizes = tokens.buttonSizes;
export const iconButtonSizes = tokens.iconButtonSizes;
export const searchBarSizes = tokens.searchBarSizes;
export const cardSizes = tokens.cardSizes;
export const rowHeights = tokens.rowHeights;

export type DesignTokens = typeof tokens;
